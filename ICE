-- SAFETY CHECK: Ensure the script actually starts
print("ICE V1 BANG: Initializing...")

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local Workspace = game:GetService("Workspace")
local TextChatService = game:GetService("TextChatService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer

-- Variables
local targetName = ""
local isTrolling = false
local flingEnabled = false
local glitchIntensity = 5
local lastCheckpoint = nil
local loopConnection
local startTime = os.time() -- FIXED TYPO HERE
local midBangThread = nil 

-- Defense Variables
local antiAfkEnabled = false
local viewingPlayer = false
local antiVoidPart = nil
local antiFlingConnection = nil
local antiSitConnection = nil
local antiBangCheckpoint = nil

-- Load Rayfield UI with Error Handling
local success, Rayfield = pcall(function()
    return loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
end)

if not success or not Rayfield then
    warn("cnx mta3ek 5ayba")
    return
end

local Window = Rayfield:CreateWindow({
   Name = "ICE SCRIPT",
   LoadingTitle = "Mar7ba BIK",
   LoadingSubtitle = "NEW UPDATE V2",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false,
   Theme = "Default" 
})

-------------------------------------------------------------------
-- TAB 1: TROLL HUB
-------------------------------------------------------------------
local MainTab = Window:CreateTab("TAF3IL", 4483362458)

local function getTargetPlayer()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            if string.lower(string.sub(player.Name, 1, #targetName)) == string.lower(targetName) then
                return player
            end
        end
    end
    return nil
end

MainTab:CreateInput({
   Name = "Hna 7ot ismo",
   PlaceholderText = "Hna",
   Callback = function(Text) targetName = Text end,
})

MainTab:CreateSlider({
   Name = "Sor3a",
   Range = {16, 250},
   Increment = 1,
   CurrentValue = 16,
   Callback = function(V) 
       pcall(function() LocalPlayer.Character.Humanoid.WalkSpeed = V end) 
   end,
})

MainTab:CreateSlider({
   Name = "Tangiz",
   Range = {50, 500},
   Increment = 1,
   CurrentValue = 50,
   Callback = function(V) 
       pcall(function() 
           LocalPlayer.Character.Humanoid.UseJumpPower = true 
           LocalPlayer.Character.Humanoid.JumpPower = V 
       end) 
   end,
})

MainTab:CreateSlider({
   Name = "El 9owa mta3 taf3il",
   Range = {1, 50},
   Increment = 1,
   CurrentValue = 5,
   Callback = function(V) glitchIntensity = V end,
})

MainTab:CreateToggle({
   Name = "FLING",
   CurrentValue = false,
   Callback = function(Value) flingEnabled = Value end,
})

MainTab:CreateToggle({
   Name = "tnjm tchofo hna",
   CurrentValue = false,
   Callback = function(Value)
      viewingPlayer = Value
      local cam = Workspace.CurrentCamera
      if viewingPlayer then
          local target = getTargetPlayer()
          if target and target.Character then cam.CameraSubject = target.Character:FindFirstChild("Humanoid") end
      else
          pcall(function() cam.CameraSubject = LocalPlayer.Character:FindFirstChild("Humanoid") end)
      end
   end,
})

MainTab:CreateToggle({
   Name = "NIKOOO!!",
   CurrentValue = false,
   Callback = function(Value)
      isTrolling = Value
      local char = LocalPlayer.Character
      local root = char and char:FindFirstChild("HumanoidRootPart")
      local hum = char and char:FindFirstChild("Humanoid")

      if isTrolling then
          Rayfield:Notify({Title = "ICE V1", Content = "NIKLO OMO", Duration = 3})
          if root then lastCheckpoint = root.CFrame end
          if hum then hum.Sit = true end
          
          midBangThread = task.delay(math.random(60, 180), function()
              if isTrolling then Rayfield:Notify({Title = "ICE V1", Content = "Mat5rjch Rak rajel", Duration = 4}) end
          end)
          
          loopConnection = RunService.Heartbeat:Connect(function()
              if not isTrolling then return end
              local target = getTargetPlayer()
              
              if not target then
                  isTrolling = false
                  if loopConnection then loopConnection:Disconnect() end
                  if root and lastCheckpoint then root.CFrame = lastCheckpoint end
                  Rayfield:Notify({Title = "Target Left", Content = "Haw 5raj Mala 9ahba", Duration = 5})
                  return
              end

              if target.Character and target.Character:FindFirstChild("Head") and root then
                  for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end
                  root.CFrame = target.Character.Head.CFrame * CFrame.new(0, 0, -1.2) * CFrame.Angles(math.rad(math.random(-360,360)), math.rad(math.random(-360,360)), 0)
                  if flingEnabled then root.AssemblyAngularVelocity = Vector3.new(0, 50000, 0) end
                  if hum and not hum.Sit then hum.Sit = true end
              end
          end)
      else
          if loopConnection then loopConnection:Disconnect() end
          if midBangThread then task.cancel(midBangThread) end 
          if getTargetPlayer() then Rayfield:Notify({Title = "ICE V1", Content = "ya 5awaf 3leh 9asit 3lih?", Duration = 4}) end
          if root then 
              root.AssemblyAngularVelocity = Vector3.new(0,0,0) 
              if lastCheckpoint then root.CFrame = lastCheckpoint end 
          end
          if hum then hum.Sit = false end
      end
   end,
})

-------------------------------------------------------------------
-- TAB 2: DEFENSE
-------------------------------------------------------------------
local DefenseTab = Window:CreateTab("7imaya", 4483362458)

DefenseTab:CreateToggle({
   Name = "Anti-Sit (Improved)",
   CurrentValue = false,
   Callback = function(Value)
      if Value then
          antiSitConnection = RunService.Heartbeat:Connect(function()
              if not isTrolling and LocalPlayer.Character then
                  local hum = LocalPlayer.Character:FindFirstChild("Humanoid")
                  if hum and hum.Sit then
                      hum.Sit = false
                      for _, v in pairs(LocalPlayer.Character:GetDescendants()) do if v:IsA("SeatWeld") or v.Name == "SeatWeld" then v:Destroy() end end
                      LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0.5, 0)
                  end
              end
          end)
      else
          if antiSitConnection then antiSitConnection:Disconnect() end
      end
   end,
})

DefenseTab:CreateToggle({
   Name = "Anti-Bang (Hide in Void)",
   CurrentValue = false,
   Callback = function(Value)
      local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
      if Value and root then
          antiBangCheckpoint = root.CFrame
          root.CFrame = CFrame.new(0, 5000, 0)
          root.Anchored = true
      elseif root and antiBangCheckpoint then
          root.Anchored = false
          root.CFrame = antiBangCheckpoint
      end
   end,
})

-------------------------------------------------------------------
-- TAB 3: PLAYER INFO
-------------------------------------------------------------------
local InfoTab = Window:CreateTab("info", 4483362458)
InfoTab:CreateLabel("User: " .. LocalPlayer.Name)
local ServerLabel = InfoTab:CreateLabel("Players: " .. #Players:GetPlayers())
local UptimeLabel = InfoTab:CreateLabel("Uptime: 0s")

task.spawn(function()
    while task.wait(1) do
        ServerLabel:Set("Players: " .. #Players:GetPlayers())
        UptimeLabel:Set("Uptime: " .. math.floor(os.time() - startTime) .. "s")
    end
end)

InfoTab:CreateToggle({
   Name = "Anti-AFK",
   CurrentValue = false,
   Callback = function(V) antiAfkEnabled = V end,
})

LocalPlayer.Idled:Connect(function()
    if antiAfkEnabled then
        VirtualUser:Button2Down(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
    end
end)

-------------------------------------------------------------------
-- INITIALIZATION BLOCK
-------------------------------------------------------------------
task.spawn(function()
    -- 1. HEAL & RP IDENTITY
    pcall(function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            LocalPlayer.Character.Humanoid.Health = 100
        end
        task.wait(0.1)
        
        local RE = ReplicatedStorage:FindFirstChild("RE")
        local RPEvent = RE and RE:FindFirstChild("1RPNam1eTex1t")
        
        if RPEvent then
            RPEvent:FireServer("RolePlayName", "CLAN ZODIAK")
            task.wait(0.1)
            RPEvent:FireServer("RolePlayBio", "BY ICE")
        end
    end)
    
    task.wait(0.5)

    -- 2. CHAT SPAM
    for i = 1, 5 do
        local msg = "CLAN ZODIAK JOINED THE SERVER!"
        if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
            local channel = TextChatService.TextChannels:FindFirstChild("RBXGeneral")
            if channel then channel:SendAsync(msg) end
        elseif ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents") then
            ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(msg, "All")
        end
        task.wait(0.2)
    end
end)

Rayfield:Notify({Title = "ICE V1 BANG", Content = "Zodiak Master Loaded!", Duration = 4})
